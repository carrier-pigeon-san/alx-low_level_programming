#include "main.h"
/**
 * print_square - draws a square on the terminal
 * @size: size of the square
 *
 * Return: Always 0
 */
void print_square(int size)
{
	int i;
	int j:

	if (n > 0)
	{
		i = 0;
		while (i < n)
		{
			j = 0;
			while (j < n)
			{
				_putchar('#';
				j++;
			}
			_putchar('\n');
			i++;
		}
		_putchar('\n');
	}
	else
		_putchar('\n');
}
